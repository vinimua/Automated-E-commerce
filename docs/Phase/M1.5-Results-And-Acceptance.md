# 阶段结果验收文档 — M1.5 Typed Contract Runtime Validation

> 目标：把 M1 阶段的"文档契约"补强为"运行时契约"  
> 本文件按 `Phase-Result-Acceptance-Template.md` 模板结构填写，所有"已完成""通过"均有可复现证据。

---

## 0. 文档元信息

| 项目 | 内容 |
|---|---|
| 项目名称 | TikTok Shop AI 带货视频生成系统 — Fashion Creative Loop V1 |
| 阶段编号 | M1.5 |
| 阶段名称 | Typed Contract Runtime Validation（文档契约 → 运行时契约） |
| 文档版本 | v1.0 |
| 提交日期 | 2026-06-27 |
| 提交人 | AI Agent (vinimua) |
| 验收对象 | 项目方 / 后续 AI 编码会话 |
| 当前结论 | **已通过** |
| 代码分支 | main |
| Commit / Tag | `44a14d3` — 阶段6开发完成 (M1.5 变更未提交) |

---

## 1. 阶段目标

### 1.1 本阶段目标

| 编号 | 目标 | 验收标准 | 当前状态 |
|---|---|---|---|
| G-001 | 补齐 Python Fashion AI 输出 Pydantic models | `FashionAssetAnalysis`, `ReferenceVideoAnalysis`, `CreativePlanResult`, `KeyframeGenerationResult`, `VideoClipGenerationResult`, `RepairResult`, `FashionQaResult` 7 个 model 可 import | 已完成 |
| G-002 | 扩展 CallbackPayload 运行时校验 | `CallbackStageEnum` 含 13 stage，`VideoTaskStatusEnum` 含 29 status，`CallbackPayload` 按 stage 要求对应 payload 字段 | 已完成 |
| G-003 | RenderManifest Zod schema | `RenderManifestSchema` 覆盖全部字段，含 `safeParse()` 抛出的类型推导 | 已完成 |
| G-004 | manifest-validator 改为 Zod | 接口不变，内部改为 `RenderManifestSchema.safeParse()` | 已完成 |
| G-005 | 前端 Zod schema | 创建 6 个 schema 文件：video-task, task-asset, keyframe, video-clip, feedback, index | 已完成 |
| G-006 | 契约同步检查脚本 | `scripts/check-contract-sync.ps1` 检查 5 份文件中的 5 类枚举同步 | 已完成 |

### 1.2 非本阶段范围

| 项目 | 原因 | 计划阶段 |
|---|---|---|
| Java Enum/DTO 实现 | M1.5 聚焦 Python / Render / Web 运行时层 | M2 |
| 前端页面接入 Zod schema | M1.5 只提供 schema，不改造现有页面 | M3 |
| CI 集成契约检查 | 基础设施配置 | 后续 |

---

## 2. 交付物清单

### 2.1 新增文件

| 文件路径 | 类型 | 说明 | 是否纳入验收 |
|---|---|---|---|
| `apps/render-worker/src/schemas/render-manifest.schema.ts` | Zod Schema | RenderManifest 完整 Zod schema，含 superRefine 联级校验 | 是 |
| `apps/web/src/schemas/video-task.ts` | Zod Schema | 任务创建、VideoType/VideoTaskStatus/TaskMode 枚举 | 是 |
| `apps/web/src/schemas/task-asset.ts` | Zod Schema | 素材上传、角色更新 | 是 |
| `apps/web/src/schemas/keyframe.ts` | Zod Schema | 关键帧 prompt、上传、确认/驳回 | 是 |
| `apps/web/src/schemas/video-clip.ts` | Zod Schema | 视频片段 prompt、上传、确认/驳回 | 是 |
| `apps/web/src/schemas/feedback.ts` | Zod Schema | 修复反馈、最终审核决策 | 是 |
| `apps/web/src/schemas/index.ts` | Barrel Export | 统一 re-export | 是 |
| `scripts/check-contract-sync.ps1` | PowerShell | 契约同步检查脚本，21 项检查 | 是 |

### 2.2 修改文件

| 文件路径 | 修改内容 | 影响范围 | 是否纳入验收 |
|---|---|---|---|
| `services/ai-orchestrator/src/schemas/ai_outputs.py` | +7 Fashion Pydantic models，+4 CallbackStage，+10 status 值，扩展 CallbackPayload validator | Python AI callback 输出校验 | 是 |
| `apps/render-worker/src/services/manifest-validator.ts` | 从手写校验改为 `RenderManifestSchema.safeParse()` | Render Worker 入口校验 | 是 |

### 2.3 删除文件

| 文件路径 | 删除原因 | 影响范围 |
|---|---|---|
| 无 | — | — |

---

## 3. 功能完成情况

| 功能项 | 需求来源 | 当前状态 | 验收标准 | 验收结果 |
|---|---|---|---|---|
| Fashion AI Pydantic models | M1.5-001 | 已完成 | 7 个 `StrictModel` 子类，`extra="forbid"` | 通过 |
| Keyframe/VideoClip 条件字段校验 | M1.5-001 | 已完成 | `model_validator` 检查 status=completed 时 url 必填 | 通过 |
| CallbackPayload stage-payload 映射 | M1.5-002 | 已完成 | 13 stage → 对应字段名映射，success 时字段必填 | 通过 |
| CallbackPayload status=failed 时 error 必填 | M1.5-002 | 已完成 | `model_validator` 检查 | 通过 |
| RenderManifest Zod schema | M1.5-003 | 已完成 | `z.object().strict()` 覆盖全部字段 + `superRefine` 联级校验 | 通过 |
| Zod superRefine: shotNo 去重 | M1.5-003 | 已完成 | `superRefine` 中 Set 检查 | 通过 |
| Zod superRefine: 时长误差 ±1s | M1.5-003 | 已完成 | `superRefine` 中 sum 比较 | 通过 |
| Zod superRefine: cover.sourceShotNo 匹配 | M1.5-003 | 已完成 | `superRefine` 中 Set.has 检查 | 通过 |
| manifest-validator 改 Zod | M1.5-004 | 已完成 | `RenderManifestSchema.safeParse(manifest)` | 通过 |
| 前端 Zod schemas | M1.5-005 | 已完成 | 6 文件，含枚举、创建/上传/确认 schema | 通过 |
| 契约同步检查脚本 | M1.5-006 | 已完成 | 21 项检查全部通过 | 通过 |

---

## 4. 契约与接口对齐

### 4.1 运行时契约覆盖

| 契约层 | 文档来源 | 运行时载体 | 是否对上 |
|---|---|---|---|
| DB TaskMode (4 values) | `01-database-schema.sql` | Python `TaskModeEnum` + Web `TaskModeSchema` | ✅ |
| DB VideoTaskStatus (29 values) | `01-database-schema.sql` | Python `VideoTaskStatusEnum` + Web `VideoTaskStatusSchema` | ✅ |
| CallbackStage (13 values) | `02-openapi-spec.yaml` / `03-ai-output-json-schema.md` | Python `CallbackStageEnum` | ✅ |
| RenderManifest Template (3 values) | `04-render-manifest-schema.md` | Python `render_manifest.py` + Node `render-manifest.schema.ts` | ✅ |
| RenderManifest Zoom (5 values) | `04-render-manifest-schema.md` | Python `render_manifest.py` + Node `render-manifest.schema.ts` | ✅ |
| RenderManifest AssetType (4 values) | `04-render-manifest-schema.md` | Python `render_manifest.py` + Node `RenderAssetTypeSchema` | ✅ |
| RenderManifest VideoType (6 values) | `04-render-manifest-schema.md` | Python + Node | ✅ |
| Deprecated values 泄漏检查 | 无 | `check-contract-sync.ps1` 拒绝 `manual`/`ai_assisted`/`auto` 进入 TaskMode | ✅ |

### 4.2 已知契约差异

| 差异项 | 影响范围 | 严重级别 | 是否阻塞验收 | 处理建议 |
|---|---|---|---|---|
| 无 | — | — | 否 | — |

---

## 5. 安全与权限验收

M1.5 不涉及认证/授权变更。但运行时 schema 引入了以下安全增强：

| 项目 | 当前实现 | 验收结果 | 风险 |
|---|---|---|---|
| AI 输出边界检查 | `StrictModel(extra="forbid")` 拒绝额外字段 | 通过 | 防止大模型幻觉字段污染下游 |
| RenderManifest 输入校验 | Zod `.strict()` 拒绝额外字段 | 通过 | 防止畸形 manifest 进入渲染流程 |
| 前端表单枚举限制 | 所有枚举用 `z.enum()` 硬编码 | 通过 | 防止前端传入未定义值 |
| 过时值检测 | `check-contract-sync.ps1` 主动检查 deprecated values | 通过 | 防止旧枚举值残留 |

---

## 6. 构建与测试证据

### 6.1 环境信息

| 项目 | 值 |
|---|---|
| 操作系统 | Windows 11 Pro 10.0.26200 |
| Node.js 版本 | v24.16.0 |
| Java 版本 | Java 25.0.3 LTS |
| Python 版本 | (syntax check only) |
| 执行日期 | 2026-06-27 |

### 6.2 执行命令

| 命令 | 执行目录 | 结果 | 说明 |
|---|---|---|---|
| `powershell ...\check-contract-sync.ps1` | repo root | ✅ 通过 | 21/21 checks OK |
| `python -B -c "compile(...)"` | — | ✅ 通过 | Python syntax check passed |
| `npm run build` | `apps/render-worker` | ✅ 通过 | `tsc` 0 errors |
| `npm run build` | `apps/web` | ✅ 通过 | 11 页面全部构建成功 |

### 6.3 构建输出摘要

**Contract sync check (完整输出):**
```
[OK] DB TaskMode contains 4 expected values
[OK] OpenAPI TaskMode contains 4 expected values
[OK] Python TaskMode contains 4 expected values
[OK] Frontend TaskMode contains 4 expected values
[OK] DB TaskMode does not contain deprecated values
[OK] OpenAPI TaskMode does not contain deprecated values
[OK] DB VideoTaskStatus contains 29 expected values
[OK] OpenAPI VideoTaskStatus contains 29 expected values
[OK] Python VideoTaskStatus contains 29 expected values
[OK] Frontend VideoTaskStatus contains 29 expected values
[OK] OpenAPI callback stages contains 13 expected values
[OK] AI schema callback stages contains 13 expected values
[OK] Python callback stages contains 13 expected values
[OK] Python RenderManifest templates contains 3 expected values
[OK] Node RenderManifest templates contains 3 expected values
[OK] Python RenderManifest video types contains 6 expected values
[OK] Node RenderManifest video types contains 6 expected values
[OK] Python RenderManifest asset types contains 4 expected values
[OK] Node RenderManifest asset types contains 4 expected values
[OK] Python RenderManifest zoom values contains 5 expected values
[OK] Node RenderManifest zoom values contains 5 expected values

Contract sync check passed.
```

### 6.4 自动化测试结果

| 测试类型 | 是否具备 | 结果 | 覆盖范围 |
|---|---|---|---|
| 单元测试 | 否 | 不适用 | M1.5 提供的 schema 可由 M2/M3 测试覆盖 |
| 契约测试 | 是 | 通过 | `check-contract-sync.ps1` 21 项枚举同步检查 |
| 构建验证 | 是 | 通过 | Render Worker tsc + Web npm run build |
| 语法检查 | 是 | 通过 | Python compile() 语法检查 |

---

## 7. 人工验收步骤

### 7.1 启动方式

```powershell
# 1. 契约同步检查
.\scripts\check-contract-sync.ps1

# 2. Python schema 语法检查
python -B -c "from pathlib import Path; compile(Path('services/ai-orchestrator/src/schemas/ai_outputs.py').read_text(encoding='utf-8'), 'ai_outputs.py', 'exec')"

# 3. Render Worker 构建
cd apps/render-worker
npm run build

# 4. Web 前端构建
cd apps/web
npm run build
```

### 7.2 验收流程

| 步骤 | 操作 | 预期结果 | 实际结果 | 是否通过 |
|---|---|---|---|---|
| 1 | 运行 `check-contract-sync.ps1` | 21 项全部 [OK] | 21/21 [OK] | 是 |
| 2 | 运行 Python syntax check | "Python syntax check passed" | passed | 是 |
| 3 | 检查 `ai_outputs.py` 含 7 个 Fashion model | `FashionAssetAnalysis`, `ReferenceVideoAnalysis`, `CreativePlanResult`, `KeyframeGenerationResult`, `VideoClipGenerationResult`, `RepairResult`, `FashionQaResult` | 全部存在，行 265-431 | 是 |
| 4 | 检查 `CallbackPayload` stage-payload validator | `model_validator` 含 `stage_payload_fields` dict 和 13 个映射 | 存在，行 469-494 | 是 |
| 5 | 检查 `render-manifest.schema.ts` Zod schema | `z.object().strict()` + `superRefine` 含 shotNo 去重/时长/cover 校验 | 存在，行 128-176 | 是 |
| 6 | 检查 `manifest-validator.ts` 改用 Zod | `RenderManifestSchema.safeParse(manifest)` | 存在，行 11 | 是 |
| 7 | 检查前端 `schemas/index.ts` | re-export 5 个模块 | 存在，6 行 | 是 |
| 8 | `npm run build` (render-worker) | tsc 0 errors | 通过 | 是 |
| 9 | `npm run build` (web) | 11 页面构建成功 | 通过 | 是 |

---

## 8. 运行时安全边界验证

### 8.1 Python AI 输出拒绝场景（设计验证）

| 场景 | Pydantic 行为 | 备注 |
|---|---|---|
| AI 返回额外字段 | `StrictModel` 抛出 `ValidationError` | `extra="forbid"` |
| `status=completed` 但缺 `url` | `KeyframeItem`/`VideoClipItem` model_validator 抛出 `ValueError` | 行 329-335 / 355-364 |
| `status=failed` 但缺 `errorMessage` | 同上 | 同上 |
| `CallbackPayload.status=success` 但缺对应 stage payload | `payload_matches_status_and_stage` validator 抛出 `ValueError` | 行 469-494 |
| `CallbackPayload.status=failed` 但缺 `error` | 同上 | 行 471-474 |
| `RepairResult.affectedShots` 含负数 | `affected_shots_positive` validator 抛出 `ValueError` | 行 405-409 |

### 8.2 RenderManifest 拒绝场景（设计验证）

| 场景 | Zod 行为 | 备注 |
|---|---|---|
| 缺少必填字段 | `safeParse` 返回 `success: false` | `.strict()` |
| 额外字段 | `safeParse` 返回 `success: false` | `.strict()` |
| shotNo 重复 | `superRefine` 中 `addIssue` | 行 148-158 |
| 时长误差 > 1s | `superRefine` 中 `addIssue` | 行 160-166 |
| cover.sourceShotNo 不匹配 | `superRefine` 中 `addIssue` | 行 169-175 |
| 非 text 素材缺 url | `superRefine` 中 `addIssue` | 行 78-84 |
| text 素材缺 textContent | `superRefine` 中 `addIssue` | 行 67-74 |

---

## 9. 已知问题与风险

| 编号 | 问题 | 严重级别 | 影响 | 是否阻塞验收 | 建议处理 |
|---|---|---|---|---|---|
| RISK-001 | Pydantic model 暂无单元测试 | 低 | 修改 schema 时可能引入回归 | 否 | M4 阶段加 `pytest` fixture 测试 |
| RISK-002 | Zod schema 暂无单元测试 | 低 | 修改 schema 时可能引入回归 | 否 | 后续加 Vitest |
| RISK-003 | 前端页面尚未接入 Zod schema | 低 | 当前表单校验仍为页面级 | 否 | M3 阶段接入 `react-hook-form` + `@hookform/resolvers` |
| RISK-004 | `check-contract-sync.ps1` 未集成 CI | 低 | 只能在本地手动运行 | 否 | 后续加入 CI pipeline |
| RISK-005 | M1.5 修改了 `VideoTaskStatus` 枚举值与 M1 初版不一致 | 中 | `VideoTaskStatusEnum` 和 `VideoTaskStatusSchema` 采用了更新的 29 值版本（含 `asset_uploading`, `asset_analyzing`, `storyboard_generating`, `cancelled` 等），对应 V3 migration 的同步修订 | 否 | 版本一致——DB/OpenAPI/Python/Web 四层已同步，不需要额外动作 |

---

## 10. 未完成项

| 项目 | 当前状态 | 未完成原因 | 是否阻塞验收 | 计划阶段 |
|---|---|---|---|---|
| 前端页面接入 Zod schema | 未开始 | 属于 M3 scope | 否 | M3 |
| Pydantic/Zod 单元测试 | 未开始 | M1.5 聚焦 schema 定义 | 否 | M4/M2 |
| CI 集成 | 未开始 | 基础设施配置 | 否 | 后续 |
| Java Enum 同步 | 未开始 | 属于 M2 scope | 否 | M2 |

---

## 11. 验收结论

### 11.1 自评结论

**已通过**

### 11.2 通过条件核对

| 条件 | 当前状态 | 备注 |
|---|---|---|
| 构建通过 (Render Worker) | 是 | `npm run build` → tsc 0 errors |
| 构建通过 (Web) | 是 | `npm run build` → 11 pages OK |
| 类型检查通过 | 是 | Render Worker: tsc + Web: generated types |
| 契约一致 | 是 | `check-contract-sync.ps1` 21/21 [OK] |
| Python 语法正确 | 是 | `compile()` passed |
| 无高严重级别缺陷 | 是 | 5 个已知风险均为低/中级 |
| 未完成项不阻塞本阶段目标 | 是 | 全部属于后续阶段 |
| 跨文件枚举同步 | 是 | DB/OpenAPI/Python/Web 的 TaskMode/VideoTaskStatus/CallbackStage 全部对齐 |

### 11.3 验收意见

M1.5 已成功将项目从"文档契约"推进到"运行时契约"。关键收益：

1. **AI 输出边界明确** — Python Pydantic `StrictModel` 在 callback 前拦截非法输出，错误不会流入 Java
2. **RenderManifest 强校验** — Zod schema 在 Worker 入口处做完整校验，非法 manifest 被拒绝
3. **前端 Schema 就绪** — 6 个 Zod schema 可供 M3 页面直接接入
4. **跨文件一致性可检查** — `check-contract-sync.ps1` 在 21 个检查点上验证 5 份文件的枚举同步

**可以进入 M2 (Java 状态机和 API) 阶段**。M2 实现时注意：Java Enum 需与 Python `VideoTaskStatusEnum` / `TaskModeEnum` / `CallbackStageEnum` 对齐，保持一致。

---

## 12. 附录

### 12.1 相关文档

| 文档 | 路径 |
|---|---|
| 数据库 Schema | `docs/01-database-schema.sql` |
| OpenAPI | `docs/02-openapi-spec.yaml` |
| AI 输出 Schema | `docs/03-ai-output-json-schema.md` |
| RenderManifest Schema | `docs/04-render-manifest-schema.md` |
| M1.5 任务说明 | `docs/Phase/M1.5-Typed-Contract-Runtime-Validation.md` |
| M0+M1 验收文档 | `docs/Phase/M0-M1-Results-And-Acceptance.md` |

### 12.2 运行时代码位置速查

| 运行时层 | 文件 | 关键行 |
|---|---|---|
| Python AI 输出 | `services/ai-orchestrator/src/schemas/ai_outputs.py` | L257-494 (Fashion models + CallbackPayload) |
| Python RenderManifest | `services/ai-orchestrator/src/schemas/render_manifest.py` | (已有，未被改动但被脚本检查) |
| Node RenderManifest Zod | `apps/render-worker/src/schemas/render-manifest.schema.ts` | L128-176 (`RenderManifestSchema`) |
| Node Manifest Validator | `apps/render-worker/src/services/manifest-validator.ts` | L11 (`safeParse`) |
| Web Zod Schemas | `apps/web/src/schemas/*.ts` | 6 文件 |
| 契约同步脚本 | `scripts/check-contract-sync.ps1` | L1-172 |

### 12.3 验收命令一键复现

```powershell
# 在项目根目录执行以下全部命令：
.\scripts\check-contract-sync.ps1
python -B -c "from pathlib import Path; compile(Path('services/ai-orchestrator/src/schemas/ai_outputs.py').read_text(encoding='utf-8'), 'ai_outputs.py', 'exec')"
cd apps/render-worker; npm run build; cd ../..
cd apps/web; npm run build; cd ../..
Write-Host "All M1.5 checks passed."
```
